import { View } from "react-native";

// Placeholder route so expo-router has an entry point to boot.
// Task 12 replaces this with the real feed screen.
export default function Index() {
  return <View />;
}
